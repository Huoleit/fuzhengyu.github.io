function video_start(e) {
    // e.querySelector('.two').style.opacity = "1";
    // e.querySelector('.three').style.opacity = "0";
  }
function video_stop(e) {
    // e.querySelector('.two').style.opacity = "0";
    // e.querySelector('.three').style.opacity = "1";
  }